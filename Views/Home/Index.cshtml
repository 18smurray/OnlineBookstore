@{
    ViewData["Title"] = "Home Page";
}

<!--Tells View what data type to expect Model data passed in controller to be in-->
<!--BookListViewModel compiles info from multiple models-->
@model BookListViewModel

<!--For each Book in the Model, add styling and print out property information-->
<!--Have to specify which property of the Model; Books or PagingInfo-->
@foreach (var x in Model.Books)
{
<div class="card card-outline-primary shadow p-2 mb-4 bg-light rounded">
    <div class="card-header">
        <small>ISBN: @x.ISBN</small>
    </div>
    
    <div class="card-body">
        <h3 class="card-title">
            @x.Title
            <span class="badge badge-pill badge-primary" style="float:right">
                <small>@x.Price.ToString("c")</small>
            </span>
        </h3>
        <p>Author: @x.AuthorLastName, @x.AuthorFirstName @x.AuthorMiddleName<br />
        Classification: @x.Classification; Category: @x.Classification<br />
        Publisher: @x.Publisher<br />
        Number of Pages: @x.PageCount</p>
    </div>
</div>
}

<!--Implement dynamically built navigation bar using tag helper using "page-model" attribute-->
<div page-model="@Model.PagingInfo" page-action="Index"
     page-classes-enabled="true"
     page-class="btn"
     page-class-normal="btn-outline-dark"
     page-class-selected="btn-primary"
     class="btn-group pull-right m-1">
</div>




